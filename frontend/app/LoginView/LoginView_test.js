'use strict';

describe('SENY.LoginView module', function() {

  beforeEach(module('SENY.LoginView'));

  describe('LoginView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('loginController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});